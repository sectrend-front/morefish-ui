export const LOCALE = ['zh', 'en'] // 默认选中第一个

export const SIZE = ['default', 'large', 'small'] // 默认选中第一个

export type LOCALE_TYPE = {
  zh: string
  en: string
}

export type SIZE_TYPE = {
  default: string
  large: string
  small: string
}

export const NAME = 'mf-configprovider'
